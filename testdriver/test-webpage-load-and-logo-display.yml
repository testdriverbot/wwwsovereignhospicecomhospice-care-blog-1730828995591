version: 4.0.80
steps:
  - prompt: Open the homepage.
    commands:
      - command: hover-text
        text: Home
        description: link in the header
        action: click
  - prompt: >-
      Assert that the logo is displayed.  # Use the content of the file from the
      environment
    commands:
      - command: assert
        expect: the logo is displayed
